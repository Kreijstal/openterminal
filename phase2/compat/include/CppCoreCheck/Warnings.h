#pragma once

// Windows Terminal carries the open CppCoreCheck warning constants under a
// lower-case filename. Match the Windows SDK spelling used by its sources on
// case-sensitive build hosts.
#include <CppCoreCheck/warnings.h>
