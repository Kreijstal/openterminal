# Phase 2: open build generation

Replace, stub, or disable proprietary build inputs and generate an unpackaged
Terminal build using an open-source toolchain. Each replacement must link back
to the Phase 0 inventory and relevant Phase 1 observations.
