# Research snapshots

Each committed snapshot lives below a source-project directory and an exact Git
commit. Snapshots must be generated by a checked-in harvester and contain no
redistributed binaries or package payloads.
